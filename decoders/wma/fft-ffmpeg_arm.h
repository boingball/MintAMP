/*
 * fft-ffmpeg_arm.h - empty stand-in.  fft-ffmpeg.c unconditionally includes
 * this to pull in ARM-specific asm FFT butterfly overrides; on every other
 * target (including here) it falls back to the portable C implementations
 * in fft-ffmpeg.c itself. Not part of upstream Rockbox.
 */
