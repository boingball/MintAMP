/*
 * asm_mcf5249.h - empty stand-in.  codeclib_misc.h unconditionally includes
 * this to pull in Coldfire-specific asm MULT32/XPROD macro overrides; on
 * every other target (including here) it falls back to the portable C
 * implementations in codeclib_misc.h itself. Not part of upstream Rockbox.
 */
